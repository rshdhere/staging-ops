apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: metaverse-tls
  namespace: metaverse
spec:
  secretName: metaverse-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - k8s-metaverse.raashed.xyz
